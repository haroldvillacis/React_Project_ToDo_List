import React from 'react';
//import './TodosError.css';

function TodosError(){
    
    return (
      <p>Error...</p>
       
    );
  }

export { TodosError }; //aquí exporta el objeto que por dentro tiene el componente que se quiere exportar